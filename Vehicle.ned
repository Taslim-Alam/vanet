
simple Vehicle
{
    parameters:
        @display("i=block/vehicle");
    gates:
        input in[];  // Define input gate as a vector gate
        output out[]; // Define output gate as a vector gate
}
